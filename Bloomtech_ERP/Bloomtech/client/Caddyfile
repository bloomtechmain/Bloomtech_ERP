cd Bloomtech_ERP/Bloomtech/client
rm Caddyfile

# Create the file with JUST the Caddyfile content (no bash commands)
cat > Caddyfile << 'ENDOFFILE'
:{$PORT}

root * /app/dist
try_files {path} /index.html
file_server
encode gzip

log {
    output stdout
    format console
}
ENDOFFILE
```

Or manually create a file called `Caddyfile` with ONLY this content:
```
:{$PORT}

root * /app/dist
try_files {path} /index.html
file_server
encode gzip

log {
    output stdout
    format console
}
